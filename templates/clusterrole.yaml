apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "regret.fullname" . }}
  labels:
    {{- include "regret.labels" . | nindent 4 }}
rules:
  # Manage adapter StatefulSets
  - apiGroups: ["apps"]
    resources: ["statefulsets"]
    verbs: ["create", "get", "list", "watch", "patch", "delete"]
  # Manage adapter pods
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["create", "get", "list", "watch", "delete"]
  # Manage adapter Services
  - apiGroups: [""]
    resources: ["services"]
    verbs: ["create", "get", "list", "delete"]
  # Read pod logs for diagnostics
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  # Exec into pods for chaos injection (network chaos via tc/iptables)
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create"]
